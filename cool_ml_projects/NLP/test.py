"""
refer: _2_predict_category.py

TF-idf: Term frequency-inverse document frequency. 
formula:
    for TF = sparse matrix, i.e frequency of each word w.r.t each document(instance of the dataset).lambda 
    for idf = -log(no of documents in which a particular word has appeared / total no of documents in the dataset.)
    

also refer jupyter notebook1.

Note : this directory is a combined knowledge of 2 courses.

"""

