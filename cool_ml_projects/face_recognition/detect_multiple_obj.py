# the libraries available are YOLO (You Only Look Once), and SSD(Single Shot Multibox Detector).
# watch siraj raval's video on YOLO,  and for SVM watch playlist: math of intelligence.
'''
yolo is 'tensorflow based library with trained weights, translated from a C neural network library called DARKNET'
WE NEED CYTHON to execute the file.

or we can use open cv to detect multiple objects.
the similar program is already been written in the "ML workspace".
'''
